DATA SEGMENT
MSG DB 'This is a test program','$'
DATA ENDS

STACK SEGMENT PARA STACK 'STACK'
STA DB 512 DUP(?)
TOP EQU LENGTH STA
STACK ENDS

CODE SEGMENT
	ASSUME CS:CODE, DS:DATA, ES:DATA, SS:STACK
START:
	MOV AX, DATA
	MOV DS, AX
	MOV AX, STACK
	MOV SS, AX
    	MOV DX, OFFSET MSG
	MOV AH, 09H
	INT 21H
	MOV AH, 01H
	INT 21H
	MOV AH, 4CH
	INT 21H
CODE ENDS